"""Value objects.

Currently empty; future immutable value types (e.g. ``AnimeId``,
``ProviderName``, ``EpisodeNumber``) belong here. Keeping the package
in place keeps the architecture boundary explicit even before any
value object has been extracted.
"""
