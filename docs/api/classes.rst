Data Classes
============

The :mod:`classes` module contains the data models and utility classes
used throughout the application. Note that several of these types are
re-exported by :mod:`domain.entities` (via the migration shims) so they
may be referenced from either module path.

.. automodule:: classes
   :members:
   :undoc-members:
   :show-inheritance:
